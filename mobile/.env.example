# Same Supabase project as the BrisVO website.
# EXPO_PUBLIC_ prefixed vars are inlined into the app bundle at build time,
# so only ever put the public anon key here — never the service role key.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON=
